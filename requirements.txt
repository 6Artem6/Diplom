# BPG Construction Pipeline - Requirements
# Research PoC for GUI-only Business Process Graph

# FastAPI
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Vector Storage
chromadb>=0.4.15

# Image Processing (placeholders - not used in skeleton)
opencv-python==4.6.0.66
pillow==9.5.0

# OCR: Tesseract for OCR only (not as text detector)
pytesseract>=0.3.10
# PaddleOCR: see requirements-paddle.txt; installed only when DISABLE_PADDLEOCR != 1 (Linux amd64).

# ML Models (REAL implementations)
torch>=2.1  # PyTorch for CLIP
torchvision>=0.15.0  # For image transforms
ultralytics>=8.1
  # YOLO
transformers>=4.35.0  # CLIP
sentence-transformers>=2.2.0  # MiniLM text embeddings (cleaned OCR + class)
numpy>=1.24,<2  # Pin <2 for Paddle/compat
huggingface_hub>=0.22.0  # For Hugging Face models

# Utilities
python-multipart>=0.0.6  # For file uploads

# Demo forms generator (HTML + screenshot)
playwright>=1.40.0

# Logging
# (logging is built-in, but we use structured logging)

# Optional: HDBSCAN for alternative line→block clustering (layout/block_builder)
hdbscan>=0.8.33

# Optional: DL layout detection (LayoutParser + Detectron2). Fallback: CV-based ui_region_detection.
# On Windows, detectron2 may need manual install: pip install 'git+https://github.com/facebookresearch/detectron2.git'
layoutparser>=0.3.4
# detectron2>=0.7.0
